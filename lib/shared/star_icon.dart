import 'package:flutter/material.dart';

class Staricon extends StatelessWidget {
  const Staricon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.star,
      color: Color.fromARGB(255, 227, 205, 0),
    );
  }
}
